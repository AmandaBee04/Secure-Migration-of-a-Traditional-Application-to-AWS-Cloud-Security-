# Docker environment variables — copy this to .env before running docker-compose
DB_ROOT_PASSWORD=rootsecret
DB_DATABASE=sms_db
DB_USERNAME=sms_user
DB_PASSWORD=sms_pass
PASSWORD_PEPPER=change_this_to_a_random_secret_string
